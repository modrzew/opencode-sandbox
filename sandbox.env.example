SSH_KEY_PATH=""
GIT_NAME=""
GIT_EMAIL=""
# Default agent when none is named on the command line (opencode | pi)
AGENT="opencode"
# Preflight-check that host.container.internal resolves inside the sandbox at
# startup (set to 0 to skip the ~0.5s probe).
OCSBX_HOST_CHECK="1"
